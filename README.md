# FastAPI Sample App for Render Deployment 🚀

This is a minimal FastAPI application you can deploy to Render for testing.

## 🔧 How to Deploy

1. Push this code to **GitHub** in a new repository (e.g., `fastapi-sample-app`).
2. Go to [https://render.com](https://render.com) → **New Web Service**.
3. Connect your GitHub repo.
4. Render will automatically detect the `Dockerfile`.
5. Click **Deploy**.

After deployment, open the Render URL, e.g.:
